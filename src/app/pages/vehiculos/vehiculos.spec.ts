import { VehiculosComponent } from './vehiculos';

describe('VehiculosComponent', () => {
  let component: VehiculosComponent;

  beforeEach(() => {
    component = new VehiculosComponent();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});